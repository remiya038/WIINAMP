# Windows XP風アプリ実装ガイド

`WINDOWS_XP_WINDOW_DESIGN.md` と組み合わせて使う、Windows 11向けXP風デスクトップアプリの実装テンプレートです。

## 必要な構成

```text
project/
├─ Program.cs                    # ネイティブウィンドウとOS連携
├─ index.html                    # アプリUI
├─ style.css                     # XP風の共通デザインと画面固有スタイル
├─ app.js                        # UIイベントとホスト通信
├─ app.ico                       # exe・タスクバー用アイコン
├─ WINDOWS_XP_WINDOW_DESIGN.md   # 見た目の設計仕様
└─ WINDOWS_XP_APP_PROMPT.md      # 新規作成依頼用の雛形
```

## ホスト側の必須項目

- 枠なしウィンドウにして、HTML/CSSでタイトルバーを描画する。
- `Text`（ウィンドウタイトル）とApp User Model IDをアプリ名に合わせる。これはタスクバー表示名・グループ化に影響する。
- exeに `.ico` を埋め込み、タスクバーにも同じアイコンを表示する。
- `minimize` / `close` / `drag` のメッセージを受け、OS標準の最小化・終了・移動を実行する。
- 折りたたみパネルがある場合は、Web UIの状態に合わせてクライアント領域の高さも変更する。

## UI側の必須項目

- タイトルバーは29px、右側に `−`、`□`、`×` のキャプションボタンを置く。
- 操作ボタンは21px四方・2px程度の角丸・白い縁・Luna風グラデーションにする。
- `×` は通常時もホバー時も赤系にする。
- `_` は下寄りに見えるため、最小化には `−` を使う。
- OS操作用ボタンには `aria-label` を付ける。
- 画面を縮めた場合も、再表示用の `+` など戻る操作を必ず残す。

## 完了前チェック

- タスクバー名がアプリ名になっており、別アプリ名が残っていない。
- タスクバーとexeに正しいアイコンが出る。
- `−`、`□`、`×` の位置・大きさ・ホバー色を100%/125%/150%表示で確認する。
- 閉じるだけ赤、最大化が使えない場合は見た目を保ったまま無効化する。
- 可変パネルを開閉しても、主要な操作ボタンや戻すボタンが隠れない。
- ビルド前に画面プレビューを確認し、ビルド後にexeでも同じ見た目になることを確認する。

## 配布時

- 単一exe配布にする場合は、自己完結型・single-fileで発行する。
- `README.md` に必要OS、初回起動時の注意、機能、ライセンスを記載する。
- Apple MusicやWindowsメディアセッション連携は、対象アプリ・Windowsの状態によって利用できない場合があることを明記する。
