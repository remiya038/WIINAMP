# Windows XP風アプリ作成プロンプト雛形

以下をコピーし、`[]` を埋めて利用します。

```text
Windows 11で動作する「[アプリ名]」を作成してください。

目的:
- [アプリの用途・主な機能]

技術・配布:
- Windows向けの単体exeとして起動できること
- タスクバー表示名とexe名は「[アプリ名]」
- タスクバーとexeに「[アイコンの内容]」のアイコンを設定
- 外部サービス連携: [なし / サービス名と必要な動作]

Windows XP風デザイン:
- `WINDOWS_XP_WINDOW_DESIGN.md` と `WINDOWS_XP_APP_IMPLEMENTATION.md` に従う
- 枠なしのカスタムウィンドウ、Luna Blueのタイトルバー
- 通常表示は軽い角丸、最大化時は角丸を解除
- 右上に「−」「□」「×」を置く。21px四方、少し角丸、白い縁と立体グラデーション
- 最小化はOSの最小化を実行。閉じるボタンは赤系で、ホバー時は青にせず少し明るい赤色へ変化
- 最大化を使う場合は「□」を有効にし、最大化中は同じボタンで元のサイズへ戻せること。タイトルバーのダブルクリックでも最大化・復元する
- 最大化を使わない場合も「□」を表示したまま無効化する
- タスクバー表示名は必ず「[アプリ名]」にする

画面構成:
- メイン画面: [配置したい要素]
- 設定画面: [必要な設定]
- 開閉可能なパネル: [パネル名 / 初期表示・非表示]
- 開閉時はウィンドウ高も調整し、重要なボタンや再表示ボタンを隠さない

品質条件:
- 100% / 125% / 150% DPIでボタンが欠けない
- ビルド前に見本画像またはプレビューでタイトルバーを確認する
- 新しい出力フォルダを増やさず、既存の配布先exeを更新する
- 実装後にビルドし、exeの絶対パスを提示する
```
